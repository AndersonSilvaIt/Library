name: CI/CD BookAPI

on:
  pull_request:
    types: [closed]
    branches: [main]
    paths:
      - 'BooksAPI/**'
      - '.github/workflows/bookapi-publish.yml'

jobs:
  build-and-push:
    if: ${{ github.event.pull_request.merged == true }}
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/bookapi:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Login to Docker Hub
        run: | 
          echo "${{ secrets.DOCKERHUB_TOKEN }}" \
            | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build Docker image
        working-directory: BooksAPI
        run: docker build -t $IMAGE_NAME .

      - name: Push Docker image
        run: docker push $IMAGE_NAME

  deploy-container:
    needs: build-and-push
    runs-on: [Self-hosted, Windows, X64]
    env:
      IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/bookapi:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Pull & restart container (HTTP only)
        shell: powershell
        run:
          Write-Host "Pulling image $env:IMAGE_NAME"
          docker pull $env:IMAGE_NAME"

          Write-Host "Removing any existing container named 'bookapi-container' (if present)..."
          docker rm -f bookapi-container 2>$null

          Write-Host "Starting new container on host 8086..."
          docker run -d `
            --name bookapi-container  `
            -p 8086:80 `
            -e ASPNETCORE_URLS=http://+:80`
            $env:IMAGE_NAME"

      